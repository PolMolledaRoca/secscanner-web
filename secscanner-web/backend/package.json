{
  "name": "secscanner-web-backend",
  "version": "1.0.0",
  "description": "Backend Express con cola y Socket.IO para gestionar escaneos con el binario secscanner.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "lint": "node --check server.js && node --check src/*.js",
    "build": "echo \"El backend de Express no requiere build.\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^6.10.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
